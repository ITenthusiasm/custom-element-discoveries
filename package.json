{
  "name": "custom-element-discoveries",
  "version": "1.0.0",
  "type": "module",
  "description": "A collection of my discoveries of what can be done with Custom Elements. Serves as an example of (hopefully) robust solutions to different problems.",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "prettier --ignore-path .gitignore --check . && eslint --ignore-path .gitignore . && tsc --noEmit",
    "format": "prettier --ignore-path .gitignore --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^22.13.1",
    "@typescript-eslint/eslint-plugin": "^8.24.0",
    "@typescript-eslint/parser": "^8.24.0",
    "eslint": "^8.57.1",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.5.0",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  },
  "author": "Isaiah Thomason",
  "license": "MIT"
}
